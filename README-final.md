# Мама, я гуляю - выпуск №001

Исправлено:

- выпуск теперь №001 в видимой части;
- кнопка «Поделиться» работает через плавающую панель;
- шеры: Telegram, VK, MAX через системный share, копирование ссылки;
- счётчик прочтений возвращён через `/api/views.js`;
- Arzamas возвращён на корректную ссылку `/mag/477-deniska`;
- полный текст выпуска сохранён.

Для счётчика в Vercel нужны Environment Variables:

```text
UPSTASH_REDIS_REST_URL
UPSTASH_REDIS_REST_TOKEN
```

После добавления переменных сделать Redeploy.
